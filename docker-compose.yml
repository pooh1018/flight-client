version: '3.8'

services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        - VITE_API_BASE_URL=http://47.109.24.42:8001  # 构建参数
    image: flight-client:latest
    container_name: flight-frontend
    ports:
      - "80:80"
    # 移除卷挂载，改为在Dockerfile中复制配置文件
    # volumes:
    #   - ./nginx.conf.d.default.conf:/etc/nginx/conf.d/default.conf:ro
    restart: unless-stopped
#    environment:
#      - BACKEND_API_URL=${BACKEND_API_URL:-http://13.239.30.88:8001/}  # 运行时环境变量
#      - BACKEND_API_URL=${BACKEND_API_URL:-http://localhost:8001/}  # 运行时环境变量
#    networks:
#      - flight-network

networks:
  flight-network:
    driver: bridge
